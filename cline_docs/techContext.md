# Tech Context

## Technologies Used
- **Backend:** TypeScript, Node.js (as inferred from files like `server/src/app.ts`)
- **Frontend:** React (inferred from files like `react/src/pages/*`, `react/src/components/*`)
- **Documentation Generation:** Static files and possibly a tool or library for generating documentation

## Development Setup
No specific setup noted.